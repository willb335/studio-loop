---
/**
 * Hero component - Main landing section with headline and CTA
 *
 * Features:
 * - Full viewport height background image
 * - Large, centered headline in bottom half of screen
 * - Clear call-to-action button
 * - Pixel-perfect match to duna.com hero
 * - Fully responsive typography
 */
import heroImage from '../assets/hero.png';
---

<section class="relative h-screen w-full overflow-hidden">
  <!-- Background Image - Full viewport coverage -->
  <div class="absolute inset-0 z-0">
    <img
      src={heroImage.src}
      alt="Hero background"
      class="w-full h-full object-cover"
    />
  </div>

  <!-- Content - Positioned in bottom half only -->
  <div class="relative z-10 h-full flex flex-col justify-end pb-16 sm:pb-20 lg:pb-24">
    <div class="max-w-4xl mx-auto px-6 sm:px-8 lg:px-12 text-center">
      <h1 class="text-4xl sm:text-5xl lg:text-6xl font-normal text-neutral-900 leading-[1.15] mb-6 sm:mb-8">
        The Affordable FitGrid Alternative
      </h1>
      
      <p class="text-lg sm:text-xl lg:text-2xl text-neutral-600 leading-relaxed mb-10 sm:mb-12 max-w-3xl mx-auto">
        Connect with your members through automated post-class follow-ups.
        Just $29/month. No annual contract.
      </p>
      
      <button
        onclick="document.getElementById('waitlist').scrollIntoView({behavior: 'smooth'})"
        class="inline-flex items-center justify-center px-8 py-3.5 sm:px-10 sm:py-4 bg-neutral-900 text-white text-base sm:text-lg font-medium rounded-full hover:bg-neutral-800 transition-all hover:shadow-xl focus:outline-none focus:ring-2 focus:ring-neutral-900 focus:ring-offset-2"
      >
        Join Waitlist
      </button>
    </div>
  </div>
</section>